#ifndef GET_USER_INFO_HH
#define GET_USER_INFO_HH
#include <string>

/** Portable wrapper functions to get information about the current
 * user, host, etc. */

std::string getUserName();
std::string getHostName();

#endif
